# UG Stream

Expo React Native app for streaming Ugandan radio stations.

## Run

```bash
npm install
npm start
```

Use `npm run ios`, `npm run android`, or `npm run web` to launch a specific target.

## Structure

- `App.tsx`: main app screen and player logic
- `shared/`: station data and shared types
- `assets/`: app icons and splash assets
